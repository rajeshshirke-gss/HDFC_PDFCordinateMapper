import { Routes } from '@angular/router';

import { TemplateMasterPage } from './template-master.page';

export const templateMasterRoutes: Routes = [
  { path: '', component: TemplateMasterPage }
];
