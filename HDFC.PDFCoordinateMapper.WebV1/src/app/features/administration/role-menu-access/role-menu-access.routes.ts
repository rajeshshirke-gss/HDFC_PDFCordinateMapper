import { Routes } from '@angular/router';

import { RoleMenuAccessPage } from './role-menu-access.page';

export const roleMenuAccessRoutes: Routes = [
  { path: '', component: RoleMenuAccessPage }
];
